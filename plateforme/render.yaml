services:
  - type: web
    name: learnlang
    runtime: node
    plan: free
    rootDir: plateforme
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: DATABASE_URL
        sync: false
      - key: SESSION_SECRET
        generateValue: true
      - key: NODE_ENV
        value: production
